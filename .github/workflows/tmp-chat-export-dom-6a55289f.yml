name: Temporary public ChatGPT stream export

on:
  push:
    branches:
      - tmp/chat-export-6a55289f-20260713

permissions:
  contents: write

jobs:
  export-stream:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout temporary branch
        uses: actions/checkout@v4
        with:
          ref: tmp/chat-export-6a55289f-20260713

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install parser
        run: python -m pip install --disable-pip-version-check beautifulsoup4

      - name: Inspect React Router stream
        run: python tmp_tools/inspect_stream.py

      - name: Decode linear public conversation
        run: python tmp_tools/stream_extract.py

      - name: Commit stream export result
        run: |
          git config user.name "chat-export-bot"
          git config user.email "chat-export-bot@users.noreply.github.com"
          git add tmp_stream_inspect tmp_stream_export
          git commit -m "chore(tmp): store decoded public conversation export" || true
          git push origin HEAD:tmp/chat-export-6a55289f-20260713
